name: Backend - Build and Deploy to Azure Container Apps

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'packages/shared/**'
      - '.github/workflows/backend-deploy.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: development

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Log in to Azure Container Registry
        run: |
          az acr login --name ${{ vars.ACR_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ vars.ACR_NAME }}.azurecr.io/${{ vars.BACKEND_IMAGE }}:${{ github.sha }}
            ${{ vars.ACR_NAME }}.azurecr.io/${{ vars.BACKEND_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max,ignore-error=true

      - name: Check if Container App exists
        id: check_app
        run: |
          if az containerapp show --name ${{ vars.BACKEND_APP_NAME }} --resource-group ${{ vars.RESOURCE_GROUP }} &>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Container App (if not exists)
        if: steps.check_app.outputs.exists == 'false'
        run: |
          # Step 1: Create with a public placeholder image and system-assigned identity
          echo "Creating Container App with placeholder image..."
          az containerapp create \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --environment ${{ vars.CAE_NAME }} \
            --image mcr.microsoft.com/azuredocs/containerapps-helloworld:latest \
            --target-port 80 \
            --ingress external \
            --min-replicas 0 \
            --max-replicas 1 \
            --cpu 0.5 \
            --memory 1.0Gi \
            --system-assigned

          # Get the system-assigned identity principal ID
          PRINCIPAL_ID=$(az containerapp show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query identity.principalId -o tsv)

          echo "Container App Principal ID: $PRINCIPAL_ID"
          echo ""
          echo "⚠️  IMPORTANT: Before the first deployment, you must run:"
          echo "    bash infrastructure/setup-container-app-identity.sh"
          echo ""
          echo "This script configures the managed identity permissions for ACR and Key Vault."
          echo "It only needs to be run once per Container App."
          echo ""

          # Step 2: Configure ACR registry with managed identity
          az containerapp registry set \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --server ${{ vars.ACR_NAME }}.azurecr.io \
            --identity system

          # Step 3: Configure secrets from Key Vault
          echo "Configuring secrets from Key Vault..."
          az containerapp secret set \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --secrets \
              sql-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/SqlDb-ConnectionString,identityref:system \
              redis-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Redis-ConnectionString,identityref:system \
              anthropic-api-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Claude-ApiKey,identityref:system \
              bc-tenant-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-TenantId,identityref:system \
              bc-client-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-ClientId,identityref:system \
              bc-client-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-ClientSecret,identityref:system \
              stor-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Storage-ConnectionString,identityref:system \
              session-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/SESSION-SECRET,identityref:system \
              encryption-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/ENCRYPTION-KEY,identityref:system \
              microsoft-client-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-ClientId,identityref:system \
              ms-client-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-ClientSecret,identityref:system \
              microsoft-tenant-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-TenantId,identityref:system \
              openai-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-OPENAI-ENDPOINT,identityref:system \
              azure-openai-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-OPENAI-KEY,identityref:system \
              search-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-SEARCH-ENDPOINT,identityref:system \
              azure-search-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-SEARCH-KEY,identityref:system \
              vision-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-VISION-ENDPOINT,identityref:system \
              azure-vision-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-VISION-KEY,identityref:system \
              azure-di-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/DocumentIntelligence-Endpoint,identityref:system \
              azure-di-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/DocumentIntelligence-Key,identityref:system \
              azure-audio-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-AUDIO-ENDPOINT,identityref:system \
              azure-audio-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-AUDIO-KEY,identityref:system \
              appinsights-conn=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/ApplicationInsights-ConnectionString,identityref:system \
              graph-webhook-url=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Graph-WebhookBaseUrl,identityref:system

          # Step 4: Update to the actual backend image with env vars
          # Get frontend URL for CORS configuration
          FRONTEND_FQDN=$(az containerapp show \
            --name ${{ vars.FRONTEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query properties.configuration.ingress.fqdn \
            --output tsv 2>/dev/null || echo "")

          if [ -z "$FRONTEND_FQDN" ]; then
            FRONTEND_URL="https://${{ vars.FRONTEND_DOMAIN }}"
          else
            FRONTEND_URL="https://$FRONTEND_FQDN"
          fi

          echo "Frontend URL for CORS: $FRONTEND_URL"
          echo "Updating to backend image..."

          az containerapp update \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --image ${{ vars.ACR_NAME }}.azurecr.io/${{ vars.BACKEND_IMAGE }}:${{ github.sha }} \
            --min-replicas 1 \
            --max-replicas 3 \
            --set-env-vars \
              NODE_ENV=production \
              PORT=3001 \
              DATABASE_CONNECTION_STRING=secretref:sql-conn-string \
              REDIS_CONNECTION_STRING=secretref:redis-conn-string \
              ANTHROPIC_API_KEY=secretref:anthropic-api-key \
              ANTHROPIC_MODEL=${{ vars.ANTHROPIC_MODEL }} \
              BC_TENANT_ID=secretref:bc-tenant-id \
              BC_CLIENT_ID=secretref:bc-client-id \
              BC_CLIENT_SECRET=secretref:bc-client-secret \
              BC_API_URL=https://api.businesscentral.dynamics.com/v2.0 \
              BC_ENVIRONMENT=production \
              STORAGE_CONNECTION_STRING=secretref:stor-conn-string \
              STORAGE_CONTAINER_NAME=user-files \
              APPLICATIONINSIGHTS_CONNECTION_STRING=secretref:appinsights-conn \
              APPLICATIONINSIGHTS_ENABLED=true \
              APPLICATIONINSIGHTS_SAMPLING_PERCENTAGE=100 \
              SESSION_SECRET=secretref:session-secret \
              ENCRYPTION_KEY=secretref:encryption-key \
              MICROSOFT_CLIENT_ID=secretref:microsoft-client-id \
              MICROSOFT_CLIENT_SECRET=secretref:ms-client-secret \
              MICROSOFT_TENANT_ID=secretref:microsoft-tenant-id \
              MICROSOFT_REDIRECT_URI=https://${{ vars.BACKEND_DOMAIN }}/api/auth/callback \
              AZURE_OPENAI_ENDPOINT=secretref:openai-endpoint \
              AZURE_OPENAI_KEY=secretref:azure-openai-key \
              AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small \
              AZURE_SEARCH_ENDPOINT=secretref:search-endpoint \
              AZURE_SEARCH_KEY=secretref:azure-search-key \
              AZURE_SEARCH_INDEX_NAME=file-chunks-index \
              AZURE_VISION_ENDPOINT=secretref:vision-endpoint \
              AZURE_VISION_KEY=secretref:azure-vision-key \
              AZURE_DI_ENDPOINT=secretref:azure-di-endpoint \
              AZURE_DI_KEY=secretref:azure-di-key \
              AZURE_AUDIO_ENDPOINT=secretref:azure-audio-endpoint \
              AZURE_AUDIO_KEY=secretref:azure-audio-key \
              FRONTEND_URL=$FRONTEND_URL \
              CORS_ORIGIN=$FRONTEND_URL \
              LOG_LEVEL=info \
              ENABLE_PROMPT_CACHING=true \
              ENABLE_EXTENDED_THINKING=true \
              MAX_CONTEXT_TOKENS=100000 \
              QUEUE_FILE_PROCESSING_CONCURRENCY=5 \
              QUEUE_FILE_CHUNKING_CONCURRENCY=3 \
              QUEUE_EMBEDDING_CONCURRENCY=3 \
              GRAPH_WEBHOOK_BASE_URL=secretref:graph-webhook-url \
              SYNC_POLLING_INTERVAL_MINUTES=30 \
              SUBSCRIPTION_RENEWAL_BUFFER_HOURS=48 \
              SUBSCRIPTION_MAX_DURATION_DAYS=29

          # Update ingress configuration for the backend
          az containerapp ingress update \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --target-port 3001

      - name: Configure Container App Secrets
        run: |
          echo "Configuring secrets from Key Vault..."
          az containerapp secret set \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --secrets \
              sql-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/SqlDb-ConnectionString,identityref:system \
              redis-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Redis-ConnectionString,identityref:system \
              anthropic-api-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Claude-ApiKey,identityref:system \
              bc-tenant-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-TenantId,identityref:system \
              bc-client-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-ClientId,identityref:system \
              bc-client-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/BC-ClientSecret,identityref:system \
              stor-conn-string=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Storage-ConnectionString,identityref:system \
              session-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/SESSION-SECRET,identityref:system \
              encryption-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/ENCRYPTION-KEY,identityref:system \
              microsoft-client-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-ClientId,identityref:system \
              ms-client-secret=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-ClientSecret,identityref:system \
              microsoft-tenant-id=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Microsoft-TenantId,identityref:system \
              openai-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-OPENAI-ENDPOINT,identityref:system \
              azure-openai-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-OPENAI-KEY,identityref:system \
              search-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-SEARCH-ENDPOINT,identityref:system \
              azure-search-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-SEARCH-KEY,identityref:system \
              vision-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-VISION-ENDPOINT,identityref:system \
              azure-vision-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-VISION-KEY,identityref:system \
              azure-di-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/DocumentIntelligence-Endpoint,identityref:system \
              azure-di-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/DocumentIntelligence-Key,identityref:system \
              azure-audio-endpoint=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-AUDIO-ENDPOINT,identityref:system \
              azure-audio-key=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/AZURE-AUDIO-KEY,identityref:system \
              appinsights-conn=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/ApplicationInsights-ConnectionString,identityref:system \
              graph-webhook-url=keyvaultref:https://${{ vars.KEY_VAULT_NAME }}.vault.azure.net/secrets/Graph-WebhookBaseUrl,identityref:system
          echo "✅ Secrets configured from Key Vault"

      - name: Update Container App (if exists)
        if: steps.check_app.outputs.exists == 'true'
        run: |
          # Get frontend URL for CORS configuration
          FRONTEND_FQDN=$(az containerapp show \
            --name ${{ vars.FRONTEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query properties.configuration.ingress.fqdn \
            --output tsv 2>/dev/null || echo "")

          if [ -z "$FRONTEND_FQDN" ]; then
            FRONTEND_URL="https://${{ vars.FRONTEND_DOMAIN }}"
          else
            FRONTEND_URL="https://$FRONTEND_FQDN"
          fi

          echo "Frontend URL for CORS: $FRONTEND_URL"

          az containerapp update \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --image ${{ vars.ACR_NAME }}.azurecr.io/${{ vars.BACKEND_IMAGE }}:${{ github.sha }} \
            --min-replicas 1 \
            --max-replicas 3 \
            --set-env-vars \
              NODE_ENV=production \
              PORT=3001 \
              DATABASE_CONNECTION_STRING=secretref:sql-conn-string \
              REDIS_CONNECTION_STRING=secretref:redis-conn-string \
              ANTHROPIC_API_KEY=secretref:anthropic-api-key \
              ANTHROPIC_MODEL=${{ vars.ANTHROPIC_MODEL }} \
              BC_TENANT_ID=secretref:bc-tenant-id \
              BC_CLIENT_ID=secretref:bc-client-id \
              BC_CLIENT_SECRET=secretref:bc-client-secret \
              BC_API_URL=https://api.businesscentral.dynamics.com/v2.0 \
              BC_ENVIRONMENT=production \
              STORAGE_CONNECTION_STRING=secretref:stor-conn-string \
              STORAGE_CONTAINER_NAME=user-files \
              APPLICATIONINSIGHTS_CONNECTION_STRING=secretref:appinsights-conn \
              APPLICATIONINSIGHTS_ENABLED=true \
              APPLICATIONINSIGHTS_SAMPLING_PERCENTAGE=100 \
              SESSION_SECRET=secretref:session-secret \
              ENCRYPTION_KEY=secretref:encryption-key \
              MICROSOFT_CLIENT_ID=secretref:microsoft-client-id \
              MICROSOFT_CLIENT_SECRET=secretref:ms-client-secret \
              MICROSOFT_TENANT_ID=secretref:microsoft-tenant-id \
              MICROSOFT_REDIRECT_URI=https://${{ vars.BACKEND_DOMAIN }}/api/auth/callback \
              AZURE_OPENAI_ENDPOINT=secretref:openai-endpoint \
              AZURE_OPENAI_KEY=secretref:azure-openai-key \
              AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small \
              AZURE_SEARCH_ENDPOINT=secretref:search-endpoint \
              AZURE_SEARCH_KEY=secretref:azure-search-key \
              AZURE_SEARCH_INDEX_NAME=file-chunks-index \
              AZURE_VISION_ENDPOINT=secretref:vision-endpoint \
              AZURE_VISION_KEY=secretref:azure-vision-key \
              AZURE_DI_ENDPOINT=secretref:azure-di-endpoint \
              AZURE_DI_KEY=secretref:azure-di-key \
              AZURE_AUDIO_ENDPOINT=secretref:azure-audio-endpoint \
              AZURE_AUDIO_KEY=secretref:azure-audio-key \
              FRONTEND_URL=$FRONTEND_URL \
              CORS_ORIGIN=$FRONTEND_URL \
              LOG_LEVEL=info \
              ENABLE_PROMPT_CACHING=true \
              ENABLE_EXTENDED_THINKING=true \
              MAX_CONTEXT_TOKENS=100000 \
              QUEUE_FILE_PROCESSING_CONCURRENCY=5 \
              QUEUE_FILE_CHUNKING_CONCURRENCY=3 \
              QUEUE_EMBEDDING_CONCURRENCY=3 \
              GRAPH_WEBHOOK_BASE_URL=secretref:graph-webhook-url \
              SYNC_POLLING_INTERVAL_MINUTES=30 \
              SUBSCRIPTION_RENEWAL_BUFFER_HOURS=48 \
              SUBSCRIPTION_MAX_DURATION_DAYS=29

      - name: Configure Ingress
        run: |
          echo "Ensuring ingress is configured for port 3001..."
          az containerapp ingress update \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --target-port 3001
          echo "✅ Ingress configured for port 3001"

      - name: Get Container App URL
        id: get_url
        run: |
          URL=$(az containerapp show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query properties.configuration.ingress.fqdn \
            --output tsv)
          echo "url=https://$URL" >> $GITHUB_OUTPUT

      - name: Container Diagnostics
        run: |
          echo "=== Container App Configuration ==="

          echo ""
          echo "📦 Revision Status:"
          az containerapp revision list \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query "[].{name:name, runningState:properties.runningState, trafficWeight:trafficWeight}" \
            -o table

          echo ""
          echo "🌐 Ingress Configuration:"
          az containerapp show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query "properties.configuration.ingress.{targetPort:targetPort, external:external, fqdn:fqdn}" \
            -o json

          echo ""
          echo "🔐 Secrets Configured:"
          az containerapp show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query "properties.configuration.secrets[].name" \
            -o tsv | wc -l
          echo "secrets total"

          echo ""
          echo "📝 Environment Variables:"
          az containerapp show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query "properties.template.containers[0].env[].name" \
            -o tsv | head -20
          echo "... (first 20 shown)"

      - name: Health Check
        run: |
          echo "Waiting 90 seconds for container startup..."
          sleep 90
          echo "Testing health endpoint: ${{ steps.get_url.outputs.url }}/health/liveness"

          MAX_RETRIES=5
          RETRY_DELAY=20

          for i in $(seq 1 $MAX_RETRIES); do
            echo ""
            echo "=== Attempt $i/$MAX_RETRIES ==="

            RESPONSE=$(curl -s -w "\nHTTP_CODE:%{http_code}" --max-time 30 --connect-timeout 15 "${{ steps.get_url.outputs.url }}/health/liveness" 2>&1)

            HTTP_CODE=$(echo "$RESPONSE" | grep "HTTP_CODE:" | cut -d':' -f2)
            BODY=$(echo "$RESPONSE" | grep -v "HTTP_CODE:")

            echo "HTTP Status: $HTTP_CODE"
            echo "Response Body (first 300 chars): $(echo "$BODY" | head -c 300)"

            # Check if response is valid JSON with expected structure
            if echo "$BODY" | jq -e '.status == "alive"' > /dev/null 2>&1; then
              echo ""
              echo "✅ Health check passed!"
              echo "Full response: $BODY"
              exit 0
            else
              echo "❌ Health check failed"

              # Check if it's the Azure welcome page
              if echo "$BODY" | grep -q "Azure Container Apps"; then
                echo "⚠️  Getting Azure welcome page - container not responding"
              fi

              if [ $i -lt $MAX_RETRIES ]; then
                echo "Waiting $RETRY_DELAY seconds before retry..."
                sleep $RETRY_DELAY
              fi
            fi
          done

          echo ""
          echo "❌ Health check failed after $MAX_RETRIES attempts"
          echo ""
          echo "=== DIAGNOSTIC: Container Logs (last 50 lines) ==="
          az containerapp logs show \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --tail 50 || echo "Could not fetch logs"
          echo ""
          echo "=== DIAGNOSTIC: Revision Status ==="
          az containerapp revision list \
            --name ${{ vars.BACKEND_APP_NAME }} \
            --resource-group ${{ vars.RESOURCE_GROUP }} \
            --query "[].{name:name, runningState:properties.runningState, healthState:properties.healthState, replicas:properties.replicas}" \
            -o table || echo "Could not fetch revision status"

          exit 1

      - name: Deployment Summary
        run: |
          echo "### Deployment Successful! 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Backend URL:** ${{ steps.get_url.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ vars.ACR_NAME }}.azurecr.io/${{ vars.BACKEND_IMAGE }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
