// Re-exports for consistent imports across marketing domain
// All marketing components should import GSAP from here, not from 'gsap' directly
export { gsap, useGSAP, ScrollTrigger, SplitText } from '../animations/gsap-config';
