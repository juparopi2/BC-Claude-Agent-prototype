/**
 * Settings Constants
 *
 * Re-exports from shared package for frontend convenience,
 * plus any frontend-only constants.
 *
 * @module domains/settings/constants/settings
 */

// Re-export from shared for frontend convenience
export {
  SETTINGS_THEME,
  SETTINGS_DEFAULT_THEME,
  SETTINGS_STORAGE_KEY,
  SETTINGS_API,
  SETTINGS_TAB,
} from '@bc-agent/shared';

// Frontend-only constants
export const SETTINGS_MODAL_WIDTH = 'max-w-3xl';
