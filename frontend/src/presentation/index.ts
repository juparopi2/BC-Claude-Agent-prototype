/**
 * Presentation Layer
 *
 * React components for UI rendering. Components in this layer:
 * - Are stateless or use local UI state only
 * - Use domain hooks for business logic
 * - Never import stores directly
 *
 * @module presentation
 */

export * from './chat';
